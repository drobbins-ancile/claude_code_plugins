{
    "hooks": {
        "PostToolUse": [
            {
                "matcher": "Write|Edit|MultiEdit",
                "hooks": [
                    {
                        "type": "command",
                        "command": "jq -r '.tool_input.file_path' | grep -E '\\.(cs)$' | xargs -r csharpier format || exit 2"
                    },
                    {
                        "type": "command",
                        "command": "jq -r '.tool_input.file_path' | grep -E '\\.(py)$' | xargs -r ruff format || exit 2"
                    }
                ]
            }
        ]
    }
}
